## Build

```sh
g++ -o dataset_generator dataset_generator.cpp
```

## Run

```sh
./dataset_generator 1000
```

You can use any size, like `1000`, `10000`, and so on.

This creates `dataset_1000.csv`.
